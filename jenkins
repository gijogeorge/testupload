pipeline {
    agent any
    stages {
         stage('Builde' ) {
	      steps {
		  sh 'echo "Hello World"'
		  sh '''
		      echo "Multiline shell works too"
			ls -lah
		      ...
                    }
		}
	   }

      }
